/**
 * Generated by orval v8.9.1 🍺
 * Do not edit manually.
 * Bhejna API
 * Bhejna is a high-performance WhatsApp messaging proxy and delivery engine.
It provides a contract-first API for sending messages, tracking delivery status, and receiving inbound messages via webhooks.

 * OpenAPI spec version: 1.0.0
 */
import type { MessageMediaType } from './messageMediaType';

/**
 * Descriptor for a non-text inbound message. Null for plain text and interactive replies. `type` selects which of the remaining fields are populated; media files are fetched via the id, never inlined.
 */
export interface MessageMedia {
  type?: MessageMediaType;
  /** Meta media id, for image/sticker/video/audio/document. */
  id?: string;
  mime_type?: string;
  caption?: string;
  filename?: string;
  /** Sticker only. */
  animated?: boolean;
  /** Audio only; true for a push-to-talk voice note. */
  voice?: boolean;
  latitude?: number;
  longitude?: number;
  name?: string;
  address?: string;
  /** Reaction only. */
  emoji?: string;
  /** Reaction only; the wamid the reaction applies to. */
  target?: string;
  contacts?: string[];
  /** Why Meta could not deliver a renderable message. */
  error?: string;
}
